% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cache.R
\name{qualtrics_configure_cache}
\alias{qualtrics_configure_cache}
\title{Configure the qualtRics on-disk response cache}
\usage{
qualtrics_configure_cache(
  ttl = qualtrics_cache_ttl(),
  dir = qualtrics_cache_dir()
)
}
\arguments{
\item{ttl}{Numeric. Cache time-to-live in \strong{seconds}.  Cached values older
than \code{ttl} are ignored and re-fetched from the API.  Set to \code{0} to disable
caching entirely.  Defaults to the current value of the
\code{qualtRics.cache_ttl} option, or \code{86400} (24 hours) if unset.}

\item{dir}{String. Path to the directory used for on-disk cache storage.
Defaults to the current value of the \code{qualtRics.cache_dir} option, or a
platform-appropriate user cache directory (\code{tools::R_user_dir("qualtRics", "cache")}) if unset.}
}
\value{
Invisibly, the updated \code{ttl} and \code{dir} as a named list.
}
\description{
Sets the time-to-live and/or directory for the cache used by all qualtRics
API functions, then rebuilds the internal memoised wrappers to pick up the
new settings.
}
\details{
Cache settings are stored as R options (\code{qualtRics.cache_ttl} and
\code{qualtRics.cache_dir}) and therefore apply for the lifetime of the current R
session.  To make them persistent, add the corresponding
\code{options(qualtRics.cache_ttl = ...)} call to your \code{.Rprofile}.
}
\examples{
\dontrun{
# Cache responses for 1 hour
qualtrics_configure_cache(ttl = 3600)

# Use a project-specific cache directory, valid for 12 hours
qualtrics_configure_cache(ttl = 43200, dir = "~/.cache/my_project/qualtRics")

# Disable caching for this session
qualtrics_configure_cache(ttl = 0)
}
}
